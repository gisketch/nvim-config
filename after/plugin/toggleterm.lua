require("toggleterm").setup {
    size = 80,
    shade_terminals = false,
    start_in_insert = false,
    persist_mode = true,
    -- persist_size = true,
    direction = 'vertical',
    autochdir = true,
}
